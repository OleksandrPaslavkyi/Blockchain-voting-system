# Blockchain-voting-system
Blockchainvoting system is a simple blockchain-based voting app built with Python (Flask). Each vote is stored as a transaction in a block linked by cryptographic hashes, ensuring transparency and data integrity. Users can cast votes and view all blocks in a clear web interface.
